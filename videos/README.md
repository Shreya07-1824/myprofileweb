# Videos

Place optional local portfolio videos in this folder, for example:

- `intro.mp4` for a personal intro
- `workspace-loop.mp4` for a muted background loop

The current website includes a responsive YouTube iframe demo section so the source code stays complete without shipping a large binary video file.
